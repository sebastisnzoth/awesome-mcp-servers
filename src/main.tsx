import React,{useMemo,useState} from 'react';import{createRoot}from'react-dom/client';import{Activity,Box,CheckCircle2,Copy,FolderKanban,Search,Server,Settings2,Terminal,ToggleLeft,ToggleRight}from'lucide-react';import'./styles.css';

type MCP={name:string;category:string;description:string;enabled:boolean;status:'online'|'offline'|'setup'};
const initial:MCP[]=[{name:'GitHub',category:'Development',description:'Repos, commits, issues y pull requests.',enabled:true,status:'online'},{name:'Supabase',category:'Database',description:'PostgreSQL, auth y backend para proyectos.',enabled:true,status:'online'},{name:'Playwright',category:'Testing',description:'Pruebas E2E y automatización del navegador.',enabled:true,status:'setup'},{name:'Filesystem',category:'Development',description:'Acceso controlado a archivos del proyecto.',enabled:true,status:'online'},{name:'Maps',category:'Location',description:'Geolocalización y herramientas cartográficas.',enabled:false,status:'offline'},{name:'Sentry',category:'Observability',description:'Errores, trazas y diagnóstico de producción.',enabled:false,status:'offline'}];
function App(){const[mcp,setMcp]=useState(initial);const[q,setQ]=useState('');const[project,setProject]=useState('UGO Desarrollo');const filtered=useMemo(()=>mcp.filter(x=>(x.name+' '+x.category).toLowerCase().includes(q.toLowerCase())),[mcp,q]);const active=mcp.filter(x=>x.enabled).length;const errors=mcp.filter(x=>x.enabled&&x.status!=='online').length;const toggle=(name:string)=>setMcp(v=>v.map(x=>x.name===name?{...x,enabled:!x.enabled}:x));const config=JSON.stringify({project,mcpServers:Object.fromEntries(mcp.filter(x=>x.enabled).map(x=>[x.name.toLowerCase(),{enabled:true}]))},null,2);return <div className="app"><aside><div className="brand"><div className="logo"><Server/></div><div><b>MCP Manager</b><span>Control Center</span></div></div><nav><button className="active"><Activity/>Dashboard</button><button><Box/>Catálogo</button><button><FolderKanban/>Proyectos</button><button><Terminal/>Configuración</button><button><Settings2/>Diagnóstico</button></nav><div className="sidefoot">MVP · v0.1</div></aside><main><header><div><p className="eyebrow">CONTROL CENTER</p><h1>Dashboard</h1><p>Administrá tus servidores MCP por proyecto.</p></div><select value={project} onChange={e=>setProject(e.target.value)}><option>UGO Desarrollo</option><option>UGO Testing</option><option>UGO Producción</option><option>Diseño</option></select></header><section className="stats"><Card n={mcp.length} label="MCP disponibles"/><Card n={active} label="Activos"/><Card n={errors} label="Requieren atención"/><Card n={1} label="Proyecto actual"/></section><section className="panel"><div className="panelhead"><div><h2>Stack MCP</h2><p>Activá únicamente los servidores que necesita {project}.</p></div><div className="search"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar MCP..."/></div></div><div className="grid">{filtered.map(x=><article key={x.name}><div className="row"><div className="icon"><Server/></div><button className="toggle" onClick={()=>toggle(x.name)}>{x.enabled?<ToggleRight/>:<ToggleLeft/>}</button></div><h3>{x.name}</h3><span className="tag">{x.category}</span><p>{x.description}</p><div className={'status '+x.status}><CheckCircle2/>{x.enabled?(x.status==='online'?'Conectado':'Configurar'):'Inactivo'}</div></article>)}</div></section><section className="config"><div><p className="eyebrow">CONFIG GENERATOR</p><h2>Configuración del proyecto</h2><p>Vista previa exportable. Los secretos nunca se guardan acá.</p></div><pre>{config}</pre><button onClick={()=>navigator.clipboard?.writeText(config)}><Copy/>Copiar configuración</button></section></main></div>}
function Card({n,label}:{n:number;label:string}){return <div className="card"><strong>{n}</strong><span>{label}</span></div>}createRoot(document.getElementById('root')!).render(<React.StrictMode><App/></React.StrictMode>);
